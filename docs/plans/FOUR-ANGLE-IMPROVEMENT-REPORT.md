# Four-angle improvement execution report

## Authority and status

Owner authorised end-to-end execution on 8 September 2026, with TDD, live validation and low-noise Telegram updates. Production restart/deployment remains explicitly gated until the end. Agent OS consumer and canonical shared-skill edits require separate scope approval. Canonical intent and acceptance gates: [sequence](FOUR-ANGLE-IMPROVEMENT-SEQUENCE.md).

**Current: Steps 1A/1B pushed through `9910512`; Step2 + native catalogue repair in `cbf1bed`; Step3A build identity in `677ee6f`; compiled entrypoint selector in `cb13932`; O5 bounded lag history in `c0bb077` — all pushed, hosted CI green through `cbf1bed` (later runs pending check). Compiled-selector live proof verified from artifacts: baseline compiled run served contract1.36.0 with fixture smoke passing; injected dist sentinel failed visibly; source control passed while dist broken; dist restored to matching sha256; all groups/dirs cleaned. Step3B strict wrapper and Step3C proof record are COMMITTED `c15ab85` and proven end-to-end: real compiled disposable server (fixture Command Code), identity precheck bound to the exact candidate build, provider-free turn passed, proof record verdict=passed exit=0, stopper verified group gone with honest evidence-persistence failure preserved; 17 acceptance/wrapper tests + 118 live-validation suite green. Approximately 35-40% complete; three review/correction cycles completed. Production untouched.**

## Baseline and ownership

- Starting HEAD: `ebf1608` (8 September 2026); Node `v24.20.0`; contract `1.35.0`.
- At baseline these dirty files were unowned; later explicit owner authority (recorded below) permits catalogue maintenance and staging: `server/src/command-code/command-code-model-catalog.ts`, `server/src/command-code/command-code-model-efforts.ts`.
- Whole-board and joined repo/worktree context plus two-day declaration history inspected. Current executor resolves to `pi-astrawebui-161` / session `01a0801b-e6fb-7447-bbe2-021344d4e621`; the managed and tmux rows were the same agent, not independent writers.
- Previous orchestration owner explicitly left after integration/deployment work. Its integrated source acceptance is recorded in [orchestration report](execution-reports/ORCHESTRATION-REPAIR-2026-09-07.md), through `c7c902c`; preserve watch generation, goal, capacity, SSE and teardown ownership. This programme does not repeat that long-horizon proof or certify production status.
- Parent owns integration and final verification. Following the owner's cost/parallelism request, two configured Luna/max worker children initially owned disjoint implementation (both have since handed back) in detached worktrees under the private operations directory: `children/registry` (only registry source/recovery tests; run `sa_mtsjrtne_20tzbl`) and `children/metrics` (only operational metrics source/receipt-metrics tests; run `sa_mtsjsauw_17inq3`). Parent retains logger/diagnostics routes and must not edit child-owned source until handback. Children run focused tests only, commit detached and do not push; parent alone integrates/pushes. Future small lookups use GPT-5.4-mini codescout rather than broad reconnaissance. No overlapping writers.
- Inherited `NODE_ENV=production` detected: tests will use explicit test mode, never source production environment files. Review fixture flags before running suites.
- Resource-scaling Phases 8–9 remain paused. No worker containment or lifecycle-controller programme is reopened.

## Baseline checks actually performed

| Command / inspection | Result | Limits |
|---|---|---|
| `npm run docs:check-agent-guides` | exit 0; byte-identical | Before this report was added |
| `npm run docs:check-links` | exit 0; 829 links / 97 Markdown files | Before this report was added |
| Root package scripts | root test and coverage still omit shared | Source evidence; sentinel proof pending |
| Git status/log and Node version | baseline above | Not a test/build pass |
| `NODE_ENV=test npm run lint` | exit 0; 1,690 warnings, zero errors | Baseline warnings retained |
| `NODE_ENV=test npm run typecheck` | exit 0 | All workspaces |
| `NODE_ENV=test npm run build` | exit 0 | Existing large-chunk warning; main client gzip 305.27 kB |
| `NODE_ENV=test OPENCODE_ENABLED=true npm run test --workspace=server -- --maxWorkers=2 --minWorkers=1` | tool deadline at 120s; no runner remained on inspection | Output reached one failed-test section; no complete pass claimed |
| Focused `command-code-core.test.ts`, one worker | exit 1; 17 passed, 1 failed | Existing unowned exclusion-list drift, detailed below |

## Disposition ledger

All mandatory items are assigned to the sequence; none is complete merely from source inspection. Current-source classification and reproduction are pending for IDs not yet inspected.

| IDs | Assigned step | Current disposition |
|---|---|---|
| S1 | 1A | copied handlers removed; 11 real-router tests pass; isolated auth mutation caught and restoration passed |
| S2 | 1A | real targets pass; isolated whole-store subscription mutations caught separately for all three components |
| V1 | 1A | permanent inventory enforcement and isolated root/coverage gates delivered; hosted CI passed |
| V2, S6 warning ratchet | 1B | actual lint rejection/acceptance proved; hosted CI34223289983 passed install/docs/lint/ratchet/typecheck/build/test/coverage |
| O1, O2 | 2A | sink/worker/byte/loss and HTTP-limit regressions pass; full snapshot gates and disposable diagnostics proof pass |
| O3 | 2B | exhaustive five-runtime mapping and actual receipt idempotency tests pass; disposable fixture API accepted1/completed1 verified |
| O4 | 2B | preserve-bytes/refuse-write/retry and caller error reporting implemented; real-file HTTP and full disposable corrupt-registry503→repair→200 proofs pass. Guard rejects non-record entries; not a comprehensive field-schema validator |
| O5 identity (3B/3C strict acceptance) | 3 | 3A COMPLETE `677ee6f`: content-fingerprinted manifest (source/config/lockfile, revision, component versions), embedded server+client identity, per-process boot ids, health exposure, failed-build no-relabel (`--noEmitOnError` + postbuild), symlink-safe walker, strict bounded projection; lifecycle pins prove stable build identity across boots with differing boot ids across two real processes. Compiled selector `cb13932` + live negative-control proof (sentinel fails visibly; source unaffected; restoration checksum match). 3B strict wrapper + 3C proof records COMPLETE `c15ab85` and live-proven: verdicts passed/failed(1)/indeterminate(2), identity precheck refusing wrong build pre-login, 17 wrapper/acceptance tests + 118 live-validation suite green, real compiled fixture run verdict=passed exit=0 with record bound to exact buildId, stopper verified |
| S3, O6 | 4 | INFRA COMPLETE `5bb79db`: disposable global-setup w/ transient auth + identity validation + verified teardown; zero-error console assertions (pre-auth /ws 403 race documented, 5/5 stable); IN FLIGHT: capability assertions, two-tab persistence, credential migration (worker bg_mtsuy6im_pp3434); diagnostics export bound COMPLETE (parent, uncommitted-to-this-row: browserDiagnostics bundle hard-bounded 128KiB with explicit truncation marker, oldest-first trim, injectable maxBytes seam, RED→GREEN 6/6); bundle measurement recorded: client main bundle 1,256,938 bytes (~1.27MB) — the plan's 128KiB bound applies to the diagnostics EXPORT bundle (enforced), not the app bundle; app code-splitting recorded as a separate finding for the operator, not a gate |
| S4, O5 history | 5 | bounded lag history COMMITTED `c0bb077`: 120-sample/60-second ring in OperationalMetrics fed by existing500ms monitor, RED→GREEN (spike retention, expiry, nearest-rank p95, reset, capacity, unchanged shed recovery, exactly one timer); real-path performance measurements remain pending |
| P1, P2 | 6 | 6A COMPLETE `b9deeb3`: exact-key registry indexes (id/path/claude/opencode/cc-native, first-match preserved, maintained across upsert-moves/delete-duplicate-restore/reload) with zero-scan cost witness; coalesced snapshot saves (in-flight + single trailing follow-up) proven by 100-concurrent-mutation latch test with at most TWO snapshot writes and durable reload of all 100; write failures reject affected waiters with retry persistence; no early acknowledgement. 5 new perf tests; 53 registry-adjacent + 3,557 full server tests green. 6B COMPLETE `f274d55` (worker-built, parent-verified + parent-added malformed-line/file-replacement regression): per-path {cached,revalidateQueued,inFlight} state machine, at most one initial full read + one trailing re-read per burst (20 full reads for ten 10-notification bursts vs ~100 old; 100 bounded header reads separately), oracle-equal metadata incl. partial last line + full replacement, injectable reader/debounce seams, debug counters, stop clears state with no stale resurrection, real chokidar temp-file append path exercised. 348 pi-suite tests green. Step 7A COMPLETE `366cde2` (worker-built, parent-completed: removed duplicate addMessage/updateMessage calls at 5 nested live-path sites incl. tool-result handlers; boundary assertion aligned to designed one-time baseline fallback): 1 publication/delta of affected tuple, 0 full-transcript scans per live delta (exactly ONE documented baseline scan when session sizeBytes meta absent), unchanged message identity preserved, content parts cloned, current/background projections fold from one canonical array. 945 client tests + typecheck + ratchet green. Step 4 INFRA COMPLETE `5bb79db` (third worker succeeded after two recon-only failures — narrow briefs with hard turn milestones are the fix; parent then de-flaked and hardened): tests/e2e/global-setup.ts boots the disposable fixture launcher with private HOME/TMPDIR + BOTH Pi overrides + transient random AUTH_PASSWORD (validation-server honours supplied value, fixture default preserved), validates buildIdentity.identityStatus==='known' over the Unix socket before tests, teardown via stopper + independent process-group-gone check with preserved evidence; shared login helper; core+mobile console tests now zero-unexpected-errors post-auth with narrow documented allowlist (pre-auth /ws 403 handshake race understood: client retries after login; bounded settle window, verified 5/5 stable suite runs incl. parent re-run). Remaining Step 4 items: dual-sdk capability assertions, two-tab real persistence, diagnostics export bound, credential migration of remaining specs, bundle measurement. |
| P3, P4, S6 state work | 7 | 7A COMPLETE `366cde2` (see row 6); 7B indexed replay lookup IN FLIGHT (worker bg_mtsuxrj6_9d7vce: per-fold storage-ID index, O(N+E), zero-scan witness, exact-equality fixtures) |
| P5 | 8 | COMPLETE `ccbeb7a`: 32MiB default global replay-byte budget (per-session limits preserved; cold sessions evicted whole first, then oldest events; cached byte sizes), cold-key bound 1,000 with consistent eviction across replay/rate/pending/warned maps, bounded eviction-marker map, pending deltas never outlive their session key, live terminal/control delivery never suppressed, snapshot responses carry replayStatus{incomplete} when history was evicted, exact deletion clears markers (late publish cannot resurrect pre-delete history). Aggregate gauges brokerReplayRetainedBytes/Keys/EvictedEventsTotal. 7 RED→GREEN budget tests; 860 internal-api+observability tests green; heap-plateau measurement deferred to Step 5 (serialised bytes ≠ heap claim) |
| S5, S6 pure dedup | 9 | verify private non-use; Tasks/Soon retained; dedup conditional |
| All 22 IDs | 10 | final integrated gates and independent review pending |

## Critical review and release accounting

- Completed independent review passes: **2, NEEDS CORRECTION**; parent-verified correction cycles: **2** (no subsequent reviewer ACCEPT). The focused logging review timed out but preserved three material findings; all were reproduced and corrected by parent with the worker's added regressions. Reviewer identified another omitted server collocated security test, inherited-environment risk in the new runner, missing permanent runner integration proof and several checker/schema edge cases. Parent confirmed and repaired discovery of the collocated security file: old targeted command reported no files; expanded server include runs 14 passing tests. Added private-home/allowlisted child environment and a real runner-boundary regression using a fake npm process: synthetic inherited provider key and unsafe agent path are absent, both Pi home overrides agree, all-skipped output is rejected. Unexpected report entries now reject; shared coverage excludes both test/spec sources. Focused review-correction suite: 11 pass after observed RED. First root rerun passed shared then failed safely on existing `server/.env`. Added test-only dotenv.config suppression preserving explicit parse fixtures, and disabled Vite envDir loading in all workspace configs; the operator-owned env file is untouched. New isolation regression observed RED→GREEN; combined focused tests 12 pass. Next actual isolated root run: shared 208 pass; server 3,458 pass / 1 fail. The remaining failure is the existing compact-evidence bound test (5,017 bytes versus <5,000) under private HOME; source inspection shows diagnostics trimming stops when no records remain, so metadata alone can exceed its bound. Follow-up found the canonical API docs explicitly preserve exact host paths even beyond the default-fixture budget (`INTERNAL-API.md`, Session evidence). Thus the prior interpretation as a product bound defect is withdrawn: this was a host-dependent default fixture. Pinned its registry locator to `/fixture/session-registry.json` and added a long-locator preservation test; 13 focused tests pass. Actual root rerun: shared 208, server 3,460, client 941 pass, all inventories verified; MCP exposed two hard-coded `/tmp` fixtures outside the runner's private TMPDIR. Replaced only those fixture paths with os.tmpdir joins; isolated MCP 71/71 and actual inventory command pass. Full typecheck passes. Final actual root run exits 0: shared208/server3460/client941/MCP71 = **4,680 tests, 392 required files**, all inventories verified (`step1a-root-green.log`). The one pointed reviewer revalidation timed out after 240 seconds without a usable verdict; no independent ACCEPT is claimed. Parent correction evidence closes the reproduced blockers, subject to final programme review. Scout reconnaissance is not review acceptance. Shared-workspace detector again named parent-owned test edits, not evidence of reviewer mutation.
- Shared discovery gate proof: created a temporary failing test under `shared/tests/`. Old actual shared command exited 0 (197 tests, 6 files), silently missing it. Expanded existing include to cover `tests/**/*.{test,spec}.ts`; same command exited 1 at the sentinel (208 genuine tests passed). Removed the task-owned sentinel; command exited 0 with 208 tests across 7 files, including all 11 child-surfacing tests. Added shared JSON reporter and ignored its generated report. Logs: `shared-sentinel-missed.log`, `shared-sentinel-caught.log`, `shared-discovery-green.log` in the private baseline directory. Root gate and permanent inventory rejection proof are not yet complete.
- Root `npm test` now runs shared first. Temporary failing sentinels placed separately under shared source and external test roots each made the actual root command exit 1 at the sentinel (208 genuine tests passed). Both removed; logs `root-src-sentinel.log` and `root-tests-sentinel.log`. The old root full-run negative remains limited by the pre-existing catalogue failure; old root omission was directly inspected in its script, and old shared successful omission was exercised above.
- Shared full-source coverage added with the installed matching V8 provider 1.6.1. Baseline: lines/statements 93.17%, branches 82.71%, functions 94.11%; thresholds 92.17/81.71/93.11, exactly one point below each baseline. Existing other-workspace thresholds untouched. Actual coverage command rejects an injected 100% line requirement (exit 1), then passes configured ratchet (exit 0, 208 tests). Lockfile-only offline install with lifecycle scripts disabled changed only the shared dependency entry. Private logs `shared-coverage.log`, `shared-coverage-rejection.log`, `shared-coverage-ratchet-green.log`.
- Permanent discovery checker and fixed root workspace runner added. Seven regression tests passed after observed RED. Checker compares filesystem inventory in each workspace's source/tests roots to actual Vitest assertions, rejects missing/empty/all-skipped files and failed assertions, and emits a machine-readable inventory. Root runner deletes previous reports before invocation and limits workers to two. Actual root command rejected an all-skipped temporary required shared file despite Vitest exiting successfully; sentinel removed. Integrated root rerun passed shared and then honestly failed the pre-existing catalogue assertion: server 3,429 passed / 1 failed across 290 files. Client/MCP were not reached by that root run. Logs `discovery-gate-red.log`, `discovery-gate-green.log`, `root-all-skipped-rejected.log`, `root-discovery-integration.log`.
- S1: added an unauthenticated-request regression to the copied test, observed failure (6 pass / 1 fail), then replaced copied handlers with the actual models router and real cookie/JWT verification using isolated signing configuration. Eleven tests pass: Pi capabilities/failure, missing/invalid GET+PUT auth, disabled OpenCode/Command Code, and actual PUT format semantics. External runtime services alone are mocked. In an isolated tracked-source copy, clean=11 pass; removing real router auth=9 pass/2 fail; restoring auth=11 pass. Initial mutation harness lacked root tsconfig and ran zero tests; retained as a harness failure, not RED proof. Fixed harness copied root tsconfig and asserted total/passed/failed counts. Owned temporary copies removed. Logs `models-copy-red.log`, `models-real-green.log`, `models-real-mutation-red.log` (harness failure), `models-isolated-{clean,mutated,restored}.log` (valid proof).
- Scout report classified all 22 IDs: no confirmed already-fixed items; S5 private non-use and P5 aggregate retention need deeper proof. This is source reconnaissance only. The readonly workspace checker flagged the new execution report, which was written by the parent during the child run; that shared-workspace observation does not establish a child write.
- S2 now renders actual Sidebar/NewSessionModal/FilesTab under React Profiler with StrictMode deliberately off, controlled external I/O and real Zustand stores. Three tests assert zero extra commits for unrelated state and a visible relevant update. Isolated tracked-source copy: clean3 pass; each target changed separately to a whole-store subscription produces exactly1 failed/2 passed; restored3 pass. Logs `components-{clean,sidebar-red,modal-red,files-red,restored}.log`; disposable copies removed.
- Documentation guides and 832 links pass; full lint remains zero errors/1,690 warnings, and explicit lint of both new MJS scripts passes. Canonical test guide and Command Code integration docs updated. No production runtime validation has been claimed.
- Source tested: Step1A unit/inventory/typecheck/lint/docs proof above; remaining programme gates pending. Committed/pushed: Step1A `6337f68`; subsequent work uncommitted. Consumer synchronised: not attempted. Deployed: not attempted.
- Owner subsequently confirmed GOAT premium exclusions and authorised the native catalogue/automation/test/docs repair, superseding the initial no-touch boundary for these catalogue artefacts. Also authorised inspection and autonomous inclusion of Pi-native refresh; accepted into sequence as a bounded addition after inspecting the existing generated provider. No sibling edits or live activation yet. Resources: `/root/pi-enhancement/commandcode-provider/{README.md,PLAN.md}` and `scripts/generate-commandcode-models.mjs` in that repository. Canonical case-sensitive wire IDs, route support, honoured effort and vision verdicts are distinct inputs; no blind CLI-list copying. Telegram recommendation accepted by notification API.
- Catalogue repair: dynamic policy test now preserves known GOAT exclusions and fail-open behaviour without freezing count; 99 focused tests passed. Read-only CLI discovery exposed 68 models while parser/generator produced 67: colon-qualified `meituan/longcat-2.0:free` was dropped and rejected by argument validation. Added failing discovery/argv regression, then permitted colons in the bounded exact ID grammar and made effort generator reuse the canonical parser. Regeneration now includes 68 models / 42 effort selectors using provider-free invalid-effort probes. Current docs, additional automation hardening and disposable runtime proof remain pending; no billed eligibility sweep or restart.
- Baseline failure root cause: the formerly unowned catalogue diff adds two excluded models to the 19-element list; `server/tests/unit/command-code/command-code-core.test.ts:30` still asserts length 19. Focused rerun reproduces 21 versus 19. Neither the list nor its test has been changed by this executor. Owner disposition requested before changing this policy/test; no exclusions or gate weakening applied.
- Private baseline logs: `/root/.pi-web-ui/operations/four-angle-20260908/baseline/` (lint, typecheck, build, server-tests, catalogue-failure). Focused runners overwrite workspace JSON reports; these run-specific logs preserve the initial failures.
- Metrics worker completed three owned files without a commit because its finalisation limit stopped tools. Parent reviewed the diff, copied only its tests first and reproduced2 failures/4 passes against the old four-runtime mapping, then applied the minimal source mapping:6 metrics/receipt tests plus7 diagnostics-route tests pass; canonical server typecheck exits0. The worker's Zod typecheck issue was worktree dependency layout, not an accepted source defect: canonical server has Zod3.25.76, root has4.4.3; a root-only node_modules link does not reproduce nested workspace resolution. Future writer worktrees need matching workspace dependency links or parent-canonical validation. New test uses real disk-backed RunReceiptManager lifecycle, repeated terminalisation and idempotent begin; no provider calls. Private logs `metrics-parent-{red,green,typecheck}.log`.
- Full coverage command completed once with exit 0 (`step1-coverage.log`), independently read by parent: shared208/server3465/client941/MCP71, no failures; lines/branches/functions respectively shared93.17/82.71/94.11, server79.47/78.11/83.90, client68.15/77.26/57.37, MCP94.63/80.44/88.54. All existing thresholds passed; none lowered. Subsequent root-native bridge tests are targeted-green but were added after this coverage run.
- Step2B authority question sent via Telegram: existing session-registry test explicitly requires corrupt JSON to rebuild as empty, conflicting with the plan's preserve-bytes/unavailable/refuse-write policy. Owner approved the recommended non-destructive replacement after receiving the expanded Telegram explanation (8 September 2026). This clears the recovery-policy authority gate: replace the old reset expectation with preserve-bytes/unavailable/refuse-write and repaired-file recovery. Do not alter the real registry.
- Step1B: added credential-free application workflow (locked npm install, docs/lint/typecheck/build/test/coverage and base-revision warning ratchet; deterministic smoke awaits Step3). First hosted run now inspected: [34218406573](https://github.com/valtterimelkko/pi-web-ui/actions/runs/34218406573) at `925f693` failed7 tests across5 files; install/docs/lint/ratchet/typecheck/build passed, coverage correctly skipped after test failure. Failures expose clean-runner assumptions in two Claude service fixtures (SDK availability), health-probe script fixture, socket permission assertion and hard-coded /root validation paths. This supersedes the earlier unverified-hosted status; CI is not yet accepted. The bounded CI worker returned investigation/controlled Python-dependency RED but no edits. Parent repaired all five fixtures: SDK health is explicitly mocked at the SDK boundary (restricted-PATH reproduction4 failures→14 passes); filesystem safety uses temporary production paths and injected EACCES with inode preservation, not root-dependent chmod assertions; health fixture uses Node sockets, checks setup status and cleans temporary files. Added a configurable HEALTH_NODE binary seam to the actual shell probe (production default remains /usr/bin/node), proved it is invoked; fixture paths now resolve from import.meta.url, not cwd. All32 focused tests pass with restricted PATH, and canonical server typecheck passes. Hosted confirmation obtained after resume: GitHub application run 34223289983 for 9910512 completed successfully at 12:04:43 UTC, including install, docs, lint/ratchet, typecheck, build, tests and coverage. This validates that committed revision, not the unfinished Step2 tree. The precise original health failure is not inferred solely from the worker's missing-Python experiment.
- Resume scouts identified missing real-file-to-HTTP recovery coverage and a concrete schema hole: non-record registry entries were accepted. Parent added four RED regressions (null, number, string, array), rejected these without rewriting bytes, and verified35 registry/recovery/diagnostics tests GREEN. Full record-field validation remains a separate check; this narrow guard does not claim a complete schema validator. HTTP recovery proof subsequently passed: real temporary corrupt file + real SessionRegistryManager + actual loopback HTTP diagnostics handler returns503 without rewriting bytes; repairing only the file makes the next HTTP request return200 with one Pi session, without replacing/resetting the manager.18 focused tests pass, with server/socket/temp-directory cleanup awaited. This is handler-level HTTP integration, not yet a full disposable application-server acceptance run (`baseline/http-registry-recovery.log`). Evidence: /tmp/registry-shape-{red,green}.log. Owner's Gmail notification was read and the useful/intentional CI purpose explained via Telegram; do not delete this verification merely to hide failures. Remote inventory: guide sync, application correctness, and two GitHub-managed Copilot review workflows; no external settings changed. CI logs are private under baseline/github-ci-34218406573.log; no email body or credentials belong in Git. New lint CLI has 5 real temporary-Git/ESLint command regressions (observed RED→GREEN), including inserted-line stability, staged rename, new unused variable, untracked file and missing base. Actual `npm run lint:ratchet -- --base HEAD` rejected a temporary new implementation unused variable and passed after removal. Broader lint inventory baseline is **1,696**, not a relaxation of the earlier 1,690: exactly six pre-existing no-console warnings in `tests/unit/debug-where.test.mjs` were outside the old root lint command. No source warning was added; existing root lint baseline remains 1,690. Logs `lint-ratchet-{red,first-green,repo,entrypoint-red,entrypoint-green}` in baseline directory. Warning matching uses per-file rule/message/source-line multisets so line shifts and detected renames preserve old debt without allowing duplicate new warnings.
- Caller worker returned reconnaissance only, no implementation. Parent added actual OpenCode completion/stale-pinned/stale-unpinned and Claude channel abort regressions; old isolated source fails4 tests (and reports unhandled rejection errors). Updated three OpenCode fire-and-forget calls to catch/error-log and upgraded six Claude channel catches from debug to correlated error notices. Corrected test synchronization to a setImmediate boundary rather than assuming one microtask sufficed.22 focused caller/recovery/OpenCode tests pass. Logs `registry-callers-{settled-red,parent-settled}.log`.
- Logging correction worker delivered six failing regressions but no source fixes. Parent reproduced and fixed credential/payload suffix variants, numeric usage-counter preservation, invalid correlation objects, inherited Error names, hostile Proxy handling and tap mutation isolation. Corrected one over-strong proposed test: V8 lazy stack formatting may read a message accessor, so that case explicitly reports unreadable stack rather than claiming to preserve an inaccessible original.65 focused tests pass; no ordinary error stack/type or valid correlation regressions observed. Logs `logger-review-parent-{red,green}.log`. Worker finalisation limits caused repeated partial deliveries; future implementation briefs should end exploration early and reserve more turns for edits/testing, not repeat the same bounded request.
- Step2 API integration now targets contract1.36.0 in source: central retention/response-truncation types are used by the actual buffer/route, canonical API/observability/contract docs describe limits and source failure. Added catalogued503 DIAGNOSTIC_SOURCE_UNAVAILABLE rather than a raw INTERNAL_ERROR literal with mismatched status. Observed RED for the actual HTTP error code and version pins, then89 focused tests GREEN and server typecheck clean. Agent OS mirror synchronisation remains a separately coordinated final migration task; production is not updated by this source change.
- Full candidate gates dispatched to isolated detached worktree `children/step2-gate` (worker sa_mtsnavud_q9dgpk). No canonical build outputs or production services should be changed. Snapshot includes current tracked diff plus five new source/test files; dependency symlinks reuse the installed root/server packages. Gate results returned and parent checked all six persisted exit-status records: docs guides/links, lint, typecheck, build and root tests exited0. Worker reports398 files /4,723 tests (208 shared,3,503 server,941 client,71 MCP). These results apply to the Step2 snapshot before the later compiled-loader edits, not the eventual combined candidate. Source-mode disposable application proof subsequently returned: real server + provider-free Command Code fixture passed all five smoke assertions; authenticated diagnostics exposed contract1.36.0, bounded retention and accepted1/completed1 counters. A second private corrupt-registry boot returned503 DIAGNOSTIC_SOURCE_UNAVAILABLE; file repair restored200/available. Parent inspected persisted responses/statuses and independently checked both successful-run process groups (3580206,3582910) have no live members and both private directories are absent. Full compiled proof remains pending. Worker reported an earlier unsuitable-environment --help attempt failed and was cleaned; this preliminary attempt is not a safe launch recipe and is retained as a first-attempt limitation.
- Parallel bounded work after Step2 integration: build/process identity implementation owns only its dedicated new helpers, root build hook, health and browser identity surfaces in `children/build-identity` (sa_mtsndz8o_riasc1). Source-mode provider-free disposable proof owns no source files in `children/step2-live` (sa_mtsnkf09_8wpubc), with allowlisted environment/private homes, explicit socket/token, fixture turn/diagnostics checks and verified existing-stopper teardown. Parent retains compiled-launcher/strict-proof integration and acceptance. Neither dispatch is completion evidence.
- Parent added `--compiled` selection to the existing launcher/child via one eight-line module loader. Five actual subprocess/module tests pass after RED: source/compiled selection, compiled-only failure while source works, missing dist with no fallback, unknown mode rejection. All17 existing process-group/stopper/exit-persistence tests pass; lint has no errors (two existing child-script warnings). No lifecycle algorithm was replaced. Full compiled application HTTP/WS/fixture proof and build identity integration remain pending; module-selection tests are not that acceptance proof. Logs: /tmp/validation-entrypoint-{red,green,lifecycle,lint}.log.
- Step3A worker returned implementation but parent has NOT integrated/accepted it. Parent review found material false-proof risks: prebuild/client-only hooks rewrite server/dist identity before successful compilation; filesystem walking follows symlinks and silently omits failed reads; relevant HTML/style/script inputs are missing; embedded metadata validation/projection is weak. Correction worker sa_mtso62mp_9f1nku owns only those identity files/build hooks in the same isolated worktree, with RED-first cases for each. This is an OPEN third review/correction cycle, not a third completed acceptance. Report's `client/src/vite.config.ts` locator was a typo: actual changed file is `client/vite.config.ts`.
- While identity correction runs, parent implemented the disjoint O5 history seam: fixed120-slot/60-second lag ring in existing OperationalMetrics, fed by existing500ms EventLoopShedMonitor. Three observed RED tests then25 related tests GREEN cover retained spike, expiry, nearest-rank p95, reset, capacity, unchanged shed recovery and exactly one existing timer/no added sampler. Current-lag field/threshold code is unchanged. This is bounded-history correctness, not completion of the fixed performance workload measurements. Logs: /tmp/event-loop-history-{red,green}.log.
- Actual compiled application source/dist negative-control proof is running in the completed gate worktree (worker sa_mtsosy1s_9t4kxh); only its compiled output may be temporarily changed/restored. No production paths are involved.
- Step2A underway: five new real-logger/direct-ring safety regressions all failed before implementation. Shared bounded projection now feeds both sink formats and diagnostics; focused logger/scrubbing tests 39/39 pass. Per-record projection is capped at8KiB, oversize strings are discarded whole before regex/format work, circular/accessor inputs are handled explicitly. Format-argument regression then exposed premature redaction of `token=%s`; observed RED and corrected, focused41 pass. Added count/byte/loss regressions (3 RED), implemented cached-byte eviction at2MiB/1000 records plus global retained-window/eviction/truncation/insertion-failure statistics; focused50 pass including a10000-record flood. Returned records are cloned to prevent callers mutating retained byte accounting. Actual SessionWorker stderr handler exercised with an inert process stream and real logger/sink/tap:8 safety tests pass. No provider process was launched. Actual HTTP flood test observed RED above1MiB; route now caps output at1MiB with explicit omitted log/error counts while preserving matching/retained counts and newest records. Byte accounting sizes each removed record once rather than reserializing the full body repeatedly. HTTP/ring tests21 pass (`diagnostics-http-{red,green}.log`); no real production server used. This historical intermediate checkpoint is superseded by the later Step2 contract/full-gate/disposable-proof evidence above.
- Next: integrate corrected identity and complete strict compiled/browser proof; bounded Pi refresh automation remains tracked alongside. Root-level Node helpers now have a required native-runner bridge in server/tests/unit/root-cli-tools.test.ts: filesystem-discovered root test/spec MJS files execute via node --test, each must report passing non-skipped assertions, no failures/cancellations. Temporary all-skipped root Node file caused the bridge test to fail; removed, both wrapper checks and the 10 native assertions pass. Initial stdin-driven programmatic node:test experiment failed in its harness; no success claim is based on it. Native assertions are not double-counted as Vitest assertions.

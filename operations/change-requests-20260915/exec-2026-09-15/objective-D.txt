The confirmation card is bound to the exact bytes the operator authorised: the proposed payload carries an identity for the text the card displayed, the confirm echoes that identity, and a confirm whose identity no longer matches the current proposal is refused and surfaced honestly without releasing anything and without consuming the draft. The `original` release variant is served only when the current proposal actually advertised it, refusing mechanically otherwise. The Internal API contract is bumped with its changelog entry and an updated drift guard, and a browser-level harness proves for the real card that the bytes released equal the bytes displayed on every path. Invariants: the verified lane behaviour on this branch does not regress, no merge, no push, no production restart, and no writes to the agent-os repository.

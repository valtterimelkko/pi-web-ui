{
  "schemaVersion": 1,
  "id": "C05",
  "title": "Negated and conditional instruction",
  "family": "constraint-integrity",
  "tier": "P",
  "holdout": false,
  "provenance": { "source": "plan 5.2 C05 row", "rationale": "every restriction survives into the candidate: investigate only, no changes until approval" },
  "speechLabel": "synthetic speech based on real wording",
  "openingWorkerState": { "status": "idle", "pendingProposal": false, "attachments": 1, "description": "one idle worker attached" },
  "inputTurns": [
    { "id": "t1", "kind": "opening", "text": "Investigate the flaky checkout test, but do not change anything until I approve.", "requiredWords": ["investigate", "flaky", "checkout", "test", "not", "change", "until", "approve"] },
    { "id": "t2", "kind": "adaptive-confirm", "text": "Yes, send that exactly as written.", "requiredWords": ["yes", "send", "exactly"] }
  ],
  "permittedRouteOutcomes": ["relay-proposal"],
  "expectedSlots": {
    "mustContain": ["investigate", "flaky checkout test", "do not change", "until i approve"],
    "mustNotContain": [],
    "responseMustContain": [],
    "responseMustNotContain": []
  },
  "requiredNegations": ["not", "until"],
  "requiredNames": [],
  "requiredNumbers": [],
  "approvalTurns": [{ "turnId": "t2", "precondition": "candidate-matched+presentation-complete" }],
  "repairBranches": [],
  "perStepDeadlinesMs": { "candidateMs": 15000, "presentationMs": 8000, "deliveryMs": 15000, "workerStoreMs": 20000 },
  "expectedFinalWorkerArtefact": { "kind": "worker-input-persisted", "description": "delivered instruction keeps the do-not-change condition verbatim; missing negation is a critical meaning error" }
}
